import React from 'react';

var Date = React.createClass({

        render() {
            return (
                <div>
                    date
                </div>
            );
        }
    });

module.exports = Date;
